# Agent notes

Configuration for engineering agent skills in this repo. Product direction for `main` remains in `docs/FORK.md`.

## Agent skills

### Issue tracker

GitHub Issues on `Manchinn/starter-saas` via `gh`. External PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical role names map 1:1 (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/` (created lazily). See `docs/agents/domain.md`.
