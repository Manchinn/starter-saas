# Product display name embedded in the client build.
# Copy to client/.env (or set in the build environment) to override the SaaS default.
VITE_APP_NAME=SaaS

# Development API proxy target.
# VITE_API_TARGET=http://localhost:3000
